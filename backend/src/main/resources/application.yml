spring:
  # ?????
  datasource:
        url: jdbc:mysql://localhost:3306/school_roll_call?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
        username: root
        password: root
        driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    # Redis??
    redis:
      host: localhost
      port: 6379
      password:
      database: 0

  # RabbitMQ??
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    virtual-host: /

  # ????
  mail:
    host: smtp.qq.com
    port: 587
    username: 596946315@qq.com
    password: lqzlmxrrwwxibece
    default-encoding: UTF-8
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

  # Thymeleaf??
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML
    encoding: UTF-8
    cache: false

# MyBatis??
mybatis:
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: com.hclx.vip2412_backend.entity
  configuration:
    map-underscore-to-camel-case: true

# PageHelper??
pagehelper:
  helper-dialect: mysql
  reasonable: true
  support-methods-arguments: true
  params: count=countSql

# ?????
server:
  port: 8080
  servlet:
    context-path: /api

# JWT??
jwt:
  secret: your-secret-key
  expiration: 86400 # 24??
  header: Authorization

# ????
logging:
  level:
    com.hclx.vip2412_backend: info
    org.springframework.security: warn